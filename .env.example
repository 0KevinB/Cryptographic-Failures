# Configuración para la versión SEGURA
# Copiar este archivo a .env y configurar valores reales

# Secret para sesiones (generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=tu_secreto_de_sesion_muy_largo_y_aleatorio_aqui

# Puerto para la aplicación segura
SECURE_PORT=3001

# Puerto para la aplicación vulnerable (SOLO DEMO)
VULNERABLE_PORT=3000

# Clave de cifrado para datos sensibles (generar con: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
ENCRYPTION_KEY=tu_clave_de_cifrado_en_base64_aqui

# Ambiente
NODE_ENV=development
